# PyGObject is inherited from the host system environment:
# PyGObject>=3.48.2 (requires --system-site-packages venv)

# Core Deep Learning & Demucs Audio Separation:
torch>=2.2.0
torchaudio>=2.2.0
demucs>=4.0.0
soundfile>=0.12.1
static-ffmpeg>=2.5
numpy>=1.26.0
scipy>=1.12.0
